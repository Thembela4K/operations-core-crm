@extends('layouts.app')

@section('content')
    <div class="flex flex-wrap items-end justify-between gap-3"><div><h1 class="page-title">Purchases</h1><p class="page-subtitle">Basic procurement tracking from approved requisitions.</p></div>@if(auth()->user()->canManageFinance())<a class="btn-primary" href="{{ route('purchases.create') }}">New Purchase</a>@endif</div>
    <form class="panel mt-6 grid gap-3 md:grid-cols-[minmax(0,1fr)_minmax(150px,180px)_auto]" method="GET"><input class="input" name="search" value="{{ request('search') }}" placeholder="Search purchase, title, or supplier"><select class="input" name="status"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status }}" @selected(request('status') === $status)>{{ $status }}</option>@endforeach</select><button class="btn-secondary" type="submit">Filter</button></form>
    <section class="panel mt-6 overflow-hidden p-0"><div class="overflow-x-auto"><table class="data-table"><thead><tr><th>Purchase</th><th>Supplier</th><th>Department</th><th>Status</th><th>Amount</th><th></th></tr></thead><tbody>@forelse($purchases as $purchase)<tr><td><strong>{{ $purchase->purchase_number }}</strong><br><span class="text-xs text-neutral-500">{{ $purchase->title }}</span></td><td>{{ $purchase->supplier?->name ?: 'No supplier' }}</td><td>{{ $purchase->department?->name ?: 'Unassigned' }}</td><td>{{ $purchase->status }}</td><td>E{{ number_format((float) $purchase->amount, 2) }}</td><td class="text-right"><a class="link" href="{{ route('purchases.show', $purchase) }}">Open</a></td></tr>@empty<tr><td colspan="6"><p class="empty">No purchases found.</p></td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $purchases->links() }}</div></section>
@endsection
